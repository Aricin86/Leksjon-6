import React from 'react';
import TodoCard from './TodoCard';

const TodoCardList = () => (
  <section>
    <TodoCard />
    <TodoCard />
    <TodoCard />
  </section>
);

export default TodoCardList;
